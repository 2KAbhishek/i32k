# Modernized i3status configuration file
# Color Palette matching BWnB Dark Theme

general {
    output_format = "i3bar"
    colors = true
    color_good = "#1688f0"
    color_degraded = "#e6cd69"
    color_bad = "#ff0043"
    interval = 2
}

order += "tztime local"
order += "cpu_usage"
order += "memory"
order += "disk /"
order += "volume master"
order += "battery all"
order += "ethernet _first_"
order += "wireless _first_"

tztime local {
    format = "󰥔 %a %b %d, %I:%M %p"
}

cpu_usage {
    format = "󰍛 %usage"
}

memory {
    format = "󰘚 %used"
    threshold_degraded = "10%"
    format_degraded = "󰘚 %available"
}

disk "/" {
    format = "󰋊 %avail"
}

volume master {
    format = "󰕾 %volume"
    format_muted = "󰝟 %volume"
    device = "default"
    mixer = "Master"
    mixer_idx = 0
}

battery all {
    format = "%status %percentage"
    format_down = "󰂎"
    status_chr = "󰂄"
    status_bat = "󰁹"
    status_unk = "󰂑"
    status_full = "󰁹"
    last_full_capacity = true
    integer_battery_capacity = true
    low_threshold = 15
    threshold_type = percentage
}

ethernet _first_ {
    format_up = "󰈀 %ip"
    format_down = ""
    color_good = "#eeeeee"
}

wireless _first_ {
    format_up = "󰤨 %essid (%quality)"
    format_down = "󰤮"
    color_good = "#eeeeee"
}
